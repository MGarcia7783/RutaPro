// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

// Rol

model Rol {
  id          String @id @default(uuid())
  nombre      String @unique
  descripcion String

  // usuarios pertenecientes al rol 
  usuarios Usuario[]
}

// Usuario

model Usuario {
  id            String   @id @default(uuid())
  nombre        String
  email         String   @unique
  passwordHash  String
  rolId         String
  activo        Boolean  @default(true)
  creadoEn      DateTime @default(now())
  actualizadoEn DateTime @updatedAt

  // Relación con el rol del usuario
  rol Rol @relation(fields: [rolId], references: [id])

  // Rutas pertenecientes al usuario
  rutas Ruta[]

  // Refresh token del usuario
  refreshTokens RefreshToken[]
}

// Refresh RefreshToken

model RefreshToken {
  id         String   @id @default(uuid())
  tokenHash  String
  usuarioId  String
  expiracion DateTime
  revocado   Boolean  @default(false)
  creadoEn   DateTime @default(now())

  usuario Usuario @relation(fields: [usuarioId], references: [id], onDelete: Cascade)

  @@index([usuarioId])
}

// Ruta de aprendizaje

model Ruta {
  id          String   @id @default(uuid())
  nombre      String   @unique
  descripcion String
  usuarioId   String
  creadaEn    DateTime @default(now())

  usuario Usuario @relation(fields: [usuarioId], references: [id])

  etapas    Etapa[]
  progresos Progreso?

  @@index([usuarioId])
}

// Etapas

model Etapa {
  id          String  @id @default(uuid())
  nombre      String
  descripcion String?
  orden       Int
  rutaId      String

  ruta      Ruta       @relation(fields: [rutaId], references: [id])
  objetivos Objetivo[]

  @@unique([rutaId, orden])
  @@unique([rutaId, nombre])
  @@index([rutaId])
}

// Objetivos

model Objetivo {
  id          String  @id @default(uuid())
  titulo      String
  descripcion String?
  etapaId     String

  etapa  Etapa   @relation(fields: [etapaId], references: [id])
  tareas Tarea[]

  @@unique([etapaId, titulo])
  @@index([etapaId])
}

// Tareas

model Tarea {
  id            String   @id @default(uuid())
  titulo        String
  descripcion   String?
  completada    Boolean  @default(false)
  objetivoId    String
  creadoEn      DateTime @default(now())
  actualizadoEn DateTime @updatedAt

  objetivo Objetivo @relation(fields: [objetivoId], references: [id])

  @@unique([objetivoId, titulo])
  @@index([objetivoId])
}

// Progreso

model Progreso {
  id            String   @id @default(uuid())
  rutaId        String   @unique
  estado        String
  porcentaje    Int      @default(0)
  creadoEn      DateTime @default(now())
  actualizadoEn DateTime @updatedAt

  ruta Ruta @relation(fields: [rutaId], references: [id])
}
